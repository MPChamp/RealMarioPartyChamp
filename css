 body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
    }

    h1, h2 {
        text-align: center;
    }

    nav {
        background-color: #333;
        color: #fff;
        text-align: center;
    }

    nav a {
        color: #fff;
        text-decoration: none;
        padding: 10px;
        display: inline-block;
    }

    nav a:hover {
        background-color: #fff;
        color: #333;
    }

    section {
        padding: 20px;
    }

    table {
        border-collapse: collapse;
        width: 100%;
        margin: auto;
    }

    th, td {
        border: 1px solid #333;
        padding: 10px;
    }
    /* Add some spacing and new styling for the leaderboard table */
    section#leaderboard table {
        margin: 20px auto;
        border: 1px solid #ccc;
        border-collapse: collapse;
        text-align: center;
    }

    /* Add a hover effect for the rows */
    section#leaderboard table tr:hover {
        background-color: #f2f2f2;
    }

    /* Style the header cells */
    section#leaderboard table th {
        border: 1px solid #ccc;
        padding: 10px;
        font-weight: bold;
        background-color: #333;
        color: #fff;
    }

    /* Style the data cells */
    section#leaderboard table td {
        border: 1px solid #ccc;
        padding: 10px;
    }

    /* Highlight the champion's row */
    section#leaderboard table td.champ {
        background-color: #ffd700;
        font-weight: bold;
    }

    img {
        width: 50%;
        height: auto;
        border: 1px solid black;
        margin: 20px 0;
    }

    img:hover {
        border: 1px solid blue;
    }
   .chart{
        width: 600px;
        height: 400px;
        border:1px solid #ccc;
    }

    /* Add some spacing and new styling for the section text */
    section p {
        margin: 20px;
        text-align: justify;
    }

    /* Use a CSS grid to layout the leaderboard table */
    section#leaderboard table {
        display: grid;
        grid-template-columns: 1fr 1fr 1fr;
        grid-row-gap: 10px;
    }

    /* Style the social media links */
    section#socials ul {
        list-style: none;
        display: flex;
        justify-content: center;
        margin: 20px;
    }

    section#socials li {
        margin: 0 10px;
    }

    section#socials a {
        color: #333;
        text-decoration: none;
    }

    section#socials a:hover {
        color: #f00;
    }
